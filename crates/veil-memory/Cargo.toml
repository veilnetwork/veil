[package]
name = "veil-memory"
version = "0.11.37"
edition = "2024"
description = "Global memory-budget manager (Epic 323): distributes a configurable RAM budget across veil components and triggers proportional eviction when exceeded.  Phase 6 extraction from veilcore."
license = "MIT OR Apache-2.0"

[dependencies]

[lints]
workspace = true
